/** Volume scale 0–10; expo-audio accepts 0.0–1.0. */
export const BGM_VOLUME_LEVEL = 2;

export const BGM_VOLUME = BGM_VOLUME_LEVEL / 10;

export const BGM_SOURCE = require('../../assets/sounds/playing.mp3');
